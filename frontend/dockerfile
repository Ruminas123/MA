FROM node:20.19.0

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .

# Expose the default Vite port
EXPOSE 3088

# ✅ Run Vite dev server on port 3088 and host 0.0.0.0
CMD ["npm", "run", "dev", "--", "--port", "3088", "--host", "0.0.0.0"]