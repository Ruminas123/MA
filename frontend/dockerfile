FROM node:20.19.0

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .

# Expose the default Vite port
EXPOSE 5173

# Use dev server instead of preview for development
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]